{
  "compilerOptions": {
    "module": "commonjs", // 包含 es6 及以上
    "target": "es5", // 编译到什么程度，什么环境下运行
    "allowJs": true, // 是否允许js语法
    "typeRoots": [
      "./node_modules/@types",
      "./typings/modules"
    ]
  },

  "include": [
    "./src/*"
  ],

  "exclude": [
    "./node_modules"
  ]
}
